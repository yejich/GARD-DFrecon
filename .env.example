# Optional: copy to .env, edit, then `set -a; source .env; set +a`.
HYPERSIM_PAIRS_ROOT=/datasets/hypersim_pairs
CUDA_VISIBLE_DEVICES=0,1
# GARD_PYTHON=/path/to/venv/bin/python
# GARD_CONFIG=run_configs/train/JIHYE_train_GARD_da3_hypersim_20k.yaml
# HF_HOME=/datasets/huggingface-cache
# Use `wandb login` or your shell's WANDB_API_KEY; no credentials in source control.
